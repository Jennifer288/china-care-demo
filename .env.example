# Leave blank for local, volatile demo mode. Use synthetic records only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Required on a deployed production server; no demo is allowed on non-loopback hosts.
APP_URL=http://localhost:3100
